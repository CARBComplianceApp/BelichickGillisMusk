{
  "name": "norcalcarbmobile-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "preview": "npx serve dist -l 4321",
    "deploy": "npm run build && npx wrangler pages deploy dist --project-name=norcalcarbmobile"
  }
}
